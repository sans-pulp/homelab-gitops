apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movies-pvc
  namespace: default
spec:
  storageClassName: "local-storage"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "1Ti"
